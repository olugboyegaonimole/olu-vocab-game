{
  "name": "vocabulary-game",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.4.0",
    "gh-pages": "^6.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.0.0"
  },
  "homepage": "https://olugboyegaonimole.github.io/olu-vocab-game",  // <-- Add this line with your GitHub Pages URL
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",  // <-- Add this line to build before deployment
    "deploy": "gh-pages -d dist"  // <-- Add this line to deploy to GitHub Pages
  }
}
